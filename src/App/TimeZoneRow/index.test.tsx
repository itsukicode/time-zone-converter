import React from 'react';
import { render } from '@testing-library/react';
import TimeZoneRow, { IProps } from './index';

// let getByTestId;
// beforeEach(() => {
//     const component = render(< TimeZoneRow selected={''} value={''} setSelected={} onChange={} />;
//     getByTestId = component.getByTestId;
// });

describe('check TimeZoneRow component works correctly', () => {
    it('should render header', () => {});
});
